package com.open592.appletviewer.events

/**
 * Base of all application events.
 *
 * Users of GlobalEventBus must provide events which extend this interface with the
 * events they wish to emit
 */
public interface ApplicationEvent
