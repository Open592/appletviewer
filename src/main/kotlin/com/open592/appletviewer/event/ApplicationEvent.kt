package com.open592.appletviewer.event

/**
 * Base of all application events.
 *
 * Users of EventBus must provide events which extend this interface with the
 * events they wish to emit
 */
public interface ApplicationEvent
